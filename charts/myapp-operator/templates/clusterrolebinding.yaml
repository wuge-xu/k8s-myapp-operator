apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "myapp-operator.fullname" . }}-manager-rolebinding
  labels:
    {{- include "myapp-operator.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "myapp-operator.fullname" . }}-manager-role
subjects:
  - kind: ServiceAccount
    name: {{ include "myapp-operator.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
